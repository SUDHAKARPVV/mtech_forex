torch>=2.2
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
statsmodels>=0.14
arch>=6.0             # AR(1)-GARCH(1,1) baseline + Hybrid's 2nd expert (baselines/garch_baseline.py)
xgboost>=2.0          # Paper 1-aligned baseline + ensemble (baselines/xgboost_baseline.py)
matplotlib>=3.8      # human-readable chart/report generation
yfinance>=0.2         # real XAU/USD candle feed (data/real_data_feed.py)
feedparser>=6.0       # real FXStreet news feed (data/real_data_feed.py)
requests>=2.28        # browser-header HTTP fetch for news feed (avoids bot-protection 403s)
streamlit>=1.59,<1.60  # interactive project dashboard (dashboard/app.py)
plotly>=5.20          # interactive charts in the dashboard

# Starlette must be capped, and streamlit pinned alongside it, because
# streamlit declares only "starlette>=0.40.0" with NO upper bound. A later
# starlette made thread_minimum_size a required keyword-only argument of
# GZipResponder.__init__, but streamlit's _MediaAwareGZipResponder subclass
# still calls super().__init__(app, minimum_size, compresslevel=...) without
# it. Nothing raises at install time; every HTTP request then dies in the gzip
# middleware and the app answers 500 to health checks and never starts. This is
# why the deploy failed while the same code ran locally -- a fresh cloud
# resolve picked up the newer starlette, the local environment had not.
# 1.59.2 + 1.3.1 is the pair verified working; revisit together, not singly.
starlette>=1.3,<1.4

# Optional, for production use (not required for the default synthetic pipeline):
# transformers>=4.30      # enables real FinBERT sentiment scoring (falls back to a lexicon scorer if absent)
# prophet>=1.1            # enables the Prophet baseline (heavy dependency, pulls in a Stan toolchain)
